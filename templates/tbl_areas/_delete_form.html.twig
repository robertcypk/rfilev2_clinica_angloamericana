<form method="post" action="{{ path('tbl_areas_delete', {'idarea': tbl_area.idarea}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tbl_area.idarea) }}">
    <button class="btn">Delete</button>
</form>